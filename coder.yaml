# yaml-language-server: $schema=https://raw.githubusercontent.com/MrPeacockNLB/schema/main/schema.json
---
coder:
  workspace:
    size: 100Mi
  #
  # VSCode (code-server)
  #
  vscode:
    extensions:
      - id: redhat.vscode-yaml # Schema Validation in VSCode
